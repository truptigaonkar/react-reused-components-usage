import React from 'react';
import { Modal } from 'react-reused-components';

const Popup = () => {
    return (
        <Modal content='This is the modal content'>Open modal</Modal>
    );
};

export default Popup;